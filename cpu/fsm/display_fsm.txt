@startuml
title Seven-Segment Display Refresh FSM

hide empty description

skinparam state {
  BackgroundColor White
  BorderColor Black
  ArrowColor Black
}

state "S_DISP_D0\nQ_cur = 00" as S_DISP_D0
state "S_DISP_D1\nQ_cur = 01" as S_DISP_D1
state "S_DISP_D2\nQ_cur = 10" as S_DISP_D2
state "S_DISP_D3\nQ_cur = 11" as S_DISP_D3

[*] --> S_DISP_D0

S_DISP_D0 --> S_DISP_D1 : refresh_tick\nenable an[0]\nshow ones / remainder

S_DISP_D1 --> S_DISP_D2 : refresh_tick\nenable an[1]\nshow tens / quotient ones\nDP on for division

S_DISP_D2 --> S_DISP_D3 : refresh_tick\nenable an[2]\nshow hundreds / quotient tens / minus

S_DISP_D3 --> S_DISP_D0 : refresh_tick\nenable an[3]\nshow minus or blank

@enduml